name: Release Check Tag Not Exists
description: Fail if the target release tag already exists.
inputs:
  release_tag:
    description: Tag to check.
    required: true
runs:
  using: node24
  main: dist/index.js
