name: Release Get Next Minor Version
description: Get the next minor version derived from the highest SemVer tag.
outputs:
  version:
    description: Next minor as a SemVer version (for example, 6.1.0).
runs:
  using: node24
  main: dist/index.js