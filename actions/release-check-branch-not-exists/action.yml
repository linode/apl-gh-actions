name: Release Check Branch Not Exists
description: Fail if the target release branch already exists on origin.
inputs:
  release_branch:
    description: Release branch name to check.
    required: true
runs:
  using: node24
  main: dist/index.js
